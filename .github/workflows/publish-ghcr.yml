name: Publish Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GH_PATH }}

    - name: Build and push web image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ghcr.io/curcolor/prontoaweb-web:latest

    - name: Build and push telegram-bot image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: Dockerfile.bot
        push: true
        tags: ghcr.io/curcolor/prontoaweb-bot:latest

    # Si usas una imagen propia para la base de datos, descomenta esta sección
    # - name: Build and push db image
    #   uses: docker/build-push-action@v4
    #   with:
    #     context: .
    #     file: Dockerfile.db
    #     push: true
    #     tags: ghcr.io/curcolor/prontoaweb-db:latest